Arduino on the Mac
##################

We can install the standard Arduino development tools on the Mac to build
projects in both ``C`` and assembly language. The installation here should be
compatible with the setup I use in a :term`virtual machine` on my Mac as well.
(see :doc:`../Linux/ArduinoOnLinux`



